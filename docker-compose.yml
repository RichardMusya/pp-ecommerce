version: '3.8'
services:
  web:
    build: .
    image: pp-ecommerce:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    # For production use a real DB (Postgres). SQLite's file will be inside container, not ideal for multi-host.
